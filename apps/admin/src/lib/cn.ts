/**
 * The class merger the vendored primitives import as `@/lib/cn`.
 *
 * It is the employee app's, re-exported rather than reimplemented: two `cn`
 * helpers that drift is a class of bug nobody ever finds.
 */
export { cn } from "@moch/ui";
